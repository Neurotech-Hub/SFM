{
  "name": "two_armed_bandit",
  "label": "Two-Armed Bandit",
  "template": "two_armed_bandit",
  "description": "Every trial, both arms run the dispense motion but only one delivers a pellet (chosen by reward probability, flipping every N trials in blocks) — the animal can't use sound or vibration alone to find the baited arm.",
  "parameters": [
    {
      "key": "nodes",
      "label": "Arms (exactly 2)",
      "type": "nodes",
      "help": "Select exactly two nodes — the two arms of the task"
    },
    {
      "key": "block_size",
      "label": "Trials per block",
      "type": "int",
      "default": 50,
      "min": 1,
      "max": 1000,
      "help": "The rich/lean arm flips after this many trials"
    },
    {
      "key": "p_high",
      "label": "Rich-arm probability",
      "type": "float",
      "default": 0.9,
      "min": 0,
      "max": 1,
      "help": "Chance the rich arm delivers on a given trial (lean arm delivers at 1 - this)"
    },
    {
      "key": "mimic",
      "label": "Mimic non-delivering nodes",
      "type": "bool",
      "default": true,
      "help": "When on, the empty arm still runs a no-feed dispense so both arms move. Off = only the fed arm moves."
    },
    {
      "key": "next_trial_wait",
      "label": "Advance to next trial",
      "type": "choice",
      "default": "presence_clear",
      "options": ["fixed_delay", "presence_clear"],
      "help": "fixed_delay = wait a fixed time; presence_clear = wait until the animal is off both presence pads"
    },
    {
      "key": "fixed_delay_s",
      "label": "Fixed delay (s)",
      "type": "float",
      "default": 5.0,
      "min": 0,
      "max": 600,
      "help": "Used when Advance = fixed_delay",
      "visible_when": {"next_trial_wait": "fixed_delay"}
    },
    {
      "key": "iti_quiet_s",
      "label": "Presence-clear settle (s)",
      "type": "float",
      "default": 1.0,
      "min": 0,
      "max": 60,
      "help": "Used when Advance = presence_clear — presence must stay clear this long before the next trial",
      "visible_when": {"next_trial_wait": "presence_clear"}
    },
    {
      "key": "minutes",
      "label": "Duration (min)",
      "type": "float",
      "default": 0,
      "help": "0 = no limit"
    },
    {
      "key": "max_pellets",
      "label": "Max pellets",
      "type": "int",
      "default": 0,
      "help": "0 = no cap"
    }
  ]
}
