"""analyses — experiment-specific METRICS, returning plain dataclasses.

Mirrors report/metrics.py's split from rendering: everything here is
testable without producing HTML (see sections/*.py for that). One module
per experiment template, named to match base_station/experiment/templates/.
"""
